VITE_API_URL=http://localhost:8000/api/v1

# Optional: when omitted, the frontend reads the public Supabase URL and anon key
# from GET /api/v1/auth/config. Never add the service-role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
