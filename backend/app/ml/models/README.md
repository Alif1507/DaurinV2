# CAMIDE model

Place the verified four-class ONNX model at `waste_classifier.onnx`. Its output order must match `CAMIDE_MODEL_LABELS`, and its preprocessing contract must accept RGB 224x224 float tensors scaled to 0-1.
